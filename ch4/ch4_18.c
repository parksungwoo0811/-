```c
#include <fcntl.h> // 파일 제어 함수를 정의하는 헤더 파일을 포함합니다.
#include <stdlib.h> // 표준 라이브러리 함수를 정의하는 헤더 파일을 포함합니다.
#include <stdio.h> // 표준 입출력 함수를 정의하는 헤더 파일을 포함합니다.

int main() {
    FILE *fp; // 파일 포인터를 선언합니다.
    int fd; // 파일 디스크립터를 저장할 변수를 선언합니다.
    char str[BUFSIZ]; // 읽은 데이터를 저장할 문자열 배열을 선언합니다.

    // 파일을 읽기 전용으로 엽니다.
    fd = open("linux.txt", O_RDONLY);
    // 파일 열기에 실패한 경우 perror 함수로 에러 메시지를 출력하고 프로그램을 종료합니다.
    if (fd == -1) {
        perror("open");
        exit(1);
    }

    // 파일 디스크립터를 이용하여 파일 스트림을 생성합니다.
    fp = fdopen(fd, "r");

    // 파일 스트림으로부터 한 줄을 읽어옵니다.
    fgets(str, BUFSIZ, fp);
    // 읽은 데이터를 출력합니다.
    printf("Read : %s\n", str);

    // 파일 스트림을 닫습니다.
    fclose(fp);
}
```

이 코드는 파일을 열어서 파일 디스크립터를 가져온 후, 파일 디스크립터를 사용하여 파일 스트림을 생성하고 파일 스트림을 이용하여 데이터를 읽어오고 출력하는 예제입니다. 먼저 파일을 읽기 전용(O_RDONLY)으로 열고, 파일 열기에 실패한 경우 perror 함수로 에러 메시지를 출력하고 프로그램을 종료합니다. 그 후 파일 디스크립터를 이용하여 파일 스트림을 생성하고, fgets 함수를 사용하여 파일 스트림으로부터 한 줄을 읽어옵니다. 읽은 데이터를 출력하고, 마지막으로 파일 스트림을 닫습니다.